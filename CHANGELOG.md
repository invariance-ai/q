# Changelog

## 0.1.0 (unreleased)

- Initial release: `q <question>` one-shot ask, multi-provider LLMs (Anthropic + OpenAI),
  user-registered HTTP API tools, regex/phrase fast-path routing, `q flag` feedback,
  feature toggles, and a polished interactive chat mode (`q` / `q chat`).
